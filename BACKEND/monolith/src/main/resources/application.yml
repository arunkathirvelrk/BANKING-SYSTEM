server:
  port: 8080

spring:
  application:
    name: banking-monolith
  datasource:
    url: jdbc:mysql://localhost:3306/banking_server?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000

# Feign configuration if needed, pointing to itself
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
